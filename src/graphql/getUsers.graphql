query GetUsers {
  Users {
    Id
    firstName
  }
}
query getCoordinates {
  postgis {
    geometry
    id
    properties
    type
  }
}

query getOneCoordinates {
  postgis(limit: 1, where: {}) {
    geometry
    id
    properties
    type
  }
}

